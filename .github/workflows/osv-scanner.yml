name: OSV-Scanner

on:
  pull_request:
    branches: ["main"]
  merge_group:
    types: [checks_requested]
  schedule:
    - cron: "12 12 * * 1"
  push:
    branches: ["main"]

permissions:
  # Required to upload SARIF file to CodeQL. See: https://github.com/github/codeql-action/issues/2117
  actions: read
  # Require writing security events to upload SARIF file to security tab
  security-events: write
  # Read commit contents
  contents: read

jobs:
  scan-scheduled:
    if: ${{ github.event_name == 'push' || github.event_name == 'schedule' }}
    uses: "google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@2c222dbe8cbd6baffa4929823c8e5c3ab481d4d0" # v2.3.2
    with:
      # Example of specifying custom arguments
      scan-args: |-
        --include-git-root
        -r
        ./
  scan-pr:
    if: ${{ github.event_name == 'pull_request' || github.event_name == 'merge_group' }}
    uses: "google/osv-scanner-action/.github/workflows/osv-scanner-reusable-pr.yml@2c222dbe8cbd6baffa4929823c8e5c3ab481d4d0" # v2.3.2
    with:
      # Example of specifying custom arguments
      scan-args: |-
        --include-git-root
        -r
        ./
