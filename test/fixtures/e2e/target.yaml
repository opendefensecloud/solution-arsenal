apiVersion: solar.opendefense.cloud/v1alpha1
kind: Target
metadata:
  name: cluster-1
  labels:
    env: prod
spec:
  renderRegistryRef:
    name: deploy-registry
  userdata:
    foo: bar
    environment: dev
