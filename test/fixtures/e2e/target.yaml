apiVersion: solar.opendefense.cloud/v1alpha1
kind: Target
metadata:
  name: cluster-1
  labels:
    env: prod
spec:
  releases:
    test-release:
      name: test-ocm-software-toi-demo-helmdemo-0-12-0-release
  userdata:
    foo: bar
    environment: dev
