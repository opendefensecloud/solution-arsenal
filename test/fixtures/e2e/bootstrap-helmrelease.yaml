---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: solar-bootstrap
spec:
  interval: 15m
  timeout: 5m
  chartRef:
    kind: OCIRepository
    name: solar-bootstrap
  values:
    userdata: {}
