---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: solar-bootstrap
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: solar-bootstrap
  install:
    remediation:
      retries: 3
    # FIXME: Enable wait again when release logic is fixed
    disableWait: true
  upgrade:
    remediation:
      retries: 3
    # FIXME: Enable wait again when release logic is fixed
    disableWait: true
  test:
    enable: true
  driftDetection:
    mode: enabled
  values:
    userdata: {}
