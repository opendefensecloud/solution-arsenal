type: generic.config.ocm.software/v1
configurations:
  - type: credentials.config.ocm.software
    consumers:
      - identity:
          type: OCIRegistry
          scheme: http
          hostname: localhost
          port: 8080
        credentials:
          - type: Credentials
            properties:
              username: admin
              password: admin
