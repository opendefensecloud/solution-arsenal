{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "github>opendefensecloud/renovate-config",
    "github>opendefensecloud/renovate-config:go",
    "github>opendefensecloud/renovate-config:k8s"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "Makefile"
      ],
      "matchStrings": [
        "CERTMANAGER_VERSION := (?<currentValue>v[0-9]+\\.[0-9]+\\.[0-9]+)"
      ],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "cert-manager/cert-manager",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "managerFilePatterns": [
        "Makefile"
      ],
      "matchStrings": [
        "TRUSTMANAGER_VERSION := (?<currentValue>v[0-9]+\\.[0-9]+\\.[0-9]+)"
      ],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "cert-manager/trust-manager",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "managerFilePatterns": [
        "Makefile"
      ],
      "matchStrings": [
        "ZOT_VERSION := (?<currentValue>[0-9]+\\.[0-9]+\\.[0-9]+)"
      ],
      "datasourceTemplate": "docker",
      "depNameTemplate": "ghcr.io/project-zot/helm-charts/zot",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "managerFilePatterns": ["assets/workflows/*.yaml"],
      "matchStrings": [
        "image:\\s*(?<depName>[a-zA-Z0-9./_-]+):(?<currentValue>[a-zA-Z0-9._-]+)"
      ],
      "datasourceTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "description": "Newer ocm-kit pseudo-versions declare go 1.27.1, which would drag go.mod past the 1.27.0 pinned in Dockerfile and flake.nix. Re-enable once go-overlay is bumped past 2026-09-01 and the toolchain moves to 1.27.1.",
      "matchManagers": [
        "gomod"
      ],
      "matchPackageNames": [
        "go.opendefense.cloud/ocm-kit"
      ],
      "enabled": false
    },
    {
      "description": "kube-openapi master tracks the next Kubernetes release and moved to structured-merge-diff/v7, but apimachinery v0.37.0 is still on v6, so managedfields fails to compile. Unpin once Kubernetes 0.38 ships.",
      "matchManagers": [
        "gomod"
      ],
      "matchPackageNames": [
        "k8s.io/kube-openapi"
      ],
      "enabled": false
    },
    {
      "description": "structured-merge-diff's major is chosen by k8s.io/code-generator: applyconfiguration-gen emits the v6 import in client-go/applyconfigurations, and apimachinery/managedfields must agree on the same major. Renovate cannot complete the bump (gomodUpdateImportPaths is off) and go mod tidy deletes the unused require. Re-enable once Kubernetes 0.38 moves both across.",
      "matchManagers": [
        "gomod"
      ],
      "matchPackageNames": [
        "sigs.k8s.io/structured-merge-diff/**"
      ],
      "enabled": false
    }
  ]
}
