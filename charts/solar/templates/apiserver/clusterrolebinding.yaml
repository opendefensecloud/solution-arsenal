{{- if and .Values.apiserver.enabled .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "solar.apiserver.fullname" . }}
  labels:
    {{- include "solar.apiserver.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "solar.apiserver.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "solar.apiserver.serviceAccountName" . }}
    namespace: {{ include "solar.namespace" . }}
{{- end }}
